import 'package:flutter/material.dart';

class AahcReport extends StatelessWidget {
  const AahcReport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
