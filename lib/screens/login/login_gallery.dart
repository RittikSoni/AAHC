import 'package:flutter/material.dart';

class LoginGallery extends StatelessWidget {
  const LoginGallery({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('under maintainance..!!'),
    );
  }
}
